# Рекомендации по хостингу для CRM системы

**Дата:** 2026-01-24  
**Целевая нагрузка:** 30-35 одновременных пользователей  
**Версия:** 1.0

---

## 1. Анализ нагрузки

### 1.1. Текущая архитектура

- **Backend:** Django 6.0 + Gunicorn (2 workers)
- **База данных:** PostgreSQL 16
- **Кэш/Broker:** Redis 7
- **Фоновые задачи:** Celery (worker concurrency=2, beat)
- **Reverse Proxy:** Nginx
- **Мобильные приложения:** Android (polling API)

### 1.2. Оценка нагрузки

#### Веб-пользователи (30-35 человек):
- **Polling уведомлений:** каждые 15 секунд = ~2.3 запроса/сек
- **CRUD операции:** создание/редактирование компаний, контактов, задач
- **Поиск по компаниям:** может быть ресурсоёмким (full-text search)
- **Dashboard:** несколько запросов к БД при загрузке
- **Аналитика:** агрегация данных по звонкам и задачам
- **Обычная нагрузка:** ~5-10 запросов/сек
- **Пиковая нагрузка:** ~20-30 запросов/сек

#### Мобильные приложения (если используются):
- **Polling звонков:** каждые 1.5-30 секунд (адаптивная частота)
- **Отправка результатов:** после каждого звонка

#### Фоновые задачи:
- **Email рассылки:** через Celery
- **Импорт из AmoCRM:** периодические задачи
- **Периодические задачи:** через Celery Beat

---

## 2. Рекомендуемые параметры хостинга

### 2.1. Минимальная конфигурация (для старта)

**VPS/Cloud Server:**
- **CPU:** 4 ядра (2.4+ GHz)
- **RAM:** 8 GB
- **Диск:** 100 GB SSD
- **Сеть:** 100 Mbps
- **ОС:** Ubuntu 22.04 LTS или Debian 12

**Обоснование:**
- **CPU (4 ядра):** 
  - Django/Gunicorn: 2 workers × 1 ядро = 2 ядра
  - PostgreSQL: 1 ядро
  - Redis + Celery: 1 ядро
  - Запас для пиковых нагрузок

- **RAM (8 GB):**
  - Django/Gunicorn: ~1.5 GB (2 workers × ~750 MB)
  - PostgreSQL: ~2-3 GB (зависит от размера БД)
  - Redis: ~500 MB
  - Celery: ~500 MB
  - Nginx: ~100 MB
  - Система: ~1 GB
  - Запас: ~1 GB

- **Диск (100 GB SSD):**
  - PostgreSQL: ~20-30 GB (зависит от объёма данных)
  - Медиафайлы: ~10-20 GB
  - Логи: ~5-10 GB
  - Система: ~20 GB
  - Запас: ~30 GB

### 2.2. Рекомендуемая конфигурация (оптимальная)

**VPS/Cloud Server:**
- **CPU:** 6-8 ядер (2.4+ GHz)
- **RAM:** 16 GB
- **Диск:** 200 GB SSD (или 100 GB SSD + отдельный диск для БД)
- **Сеть:** 1 Gbps
- **ОС:** Ubuntu 22.04 LTS или Debian 12

**Обоснование:**
- **CPU (6-8 ядер):** 
  - Больше workers для Gunicorn (3-4 workers)
  - Лучшая производительность при пиковых нагрузках
  - Параллельная обработка запросов

- **RAM (16 GB):**
  - Больше кэша для PostgreSQL
  - Больше workers для Gunicorn
  - Запас для роста данных

- **Диск (200 GB SSD):**
  - Больше места для медиафайлов и логов
  - Возможность отдельного диска для БД (лучшая производительность)

### 2.3. Конфигурация для масштабирования (если планируется рост)

**VPS/Cloud Server:**
- **CPU:** 8-12 ядер (2.4+ GHz)
- **RAM:** 32 GB
- **Диск:** 500 GB SSD (или отдельные диски для БД и медиа)
- **Сеть:** 1 Gbps
- **ОС:** Ubuntu 22.04 LTS или Debian 12

---

## 3. Оптимизация конфигурации

### 3.1. Gunicorn workers

**Текущая конфигурация:** `--workers 2`

**Рекомендация для 30-35 пользователей:**
```bash
# Формула: (2 × CPU cores) + 1
# Для 4 ядер: (2 × 4) + 1 = 9 workers (максимум)
# Для 6 ядер: (2 × 6) + 1 = 13 workers (максимум)
# Рекомендуется: 4-6 workers для старта

gunicorn crm.wsgi:application --bind 0.0.0.0:8000 --workers 4 --timeout 120 --threads 2
```

**Обоснование:**
- 2 workers может быть недостаточно для 30-35 пользователей
- 4-6 workers обеспечит лучшую параллельную обработку
- `--threads 2` для обработки I/O-bound операций (polling)

### 3.2. PostgreSQL настройки

**Рекомендуемые настройки для 8-16 GB RAM:**

```conf
# postgresql.conf
shared_buffers = 2GB              # 25% от RAM
effective_cache_size = 6GB        # 50-75% от RAM
maintenance_work_mem = 512MB
work_mem = 16MB                   # для сортировок и соединений
max_connections = 100             # достаточно для 30-35 пользователей
```

### 3.3. Redis настройки

**Рекомендуемые настройки:**

```conf
# redis.conf
maxmemory 1gb
maxmemory-policy allkeys-lru
```

### 3.4. Celery настройки

**Текущая конфигурация:** `--concurrency=2`

**Рекомендация:**
```bash
# Для 4 ядер: concurrency=4
# Для 6 ядер: concurrency=6
celery -A crm worker --loglevel=info --concurrency=4
```

---

## 4. Рекомендации по провайдерам

### 4.1. Российские провайдеры

**Timeweb Cloud:**
- **Тариф:** "Облачный сервер" 4 vCPU / 8 GB / 100 GB SSD
- **Цена:** ~1500-2000 руб/мес
- **Плюсы:** Российский провайдер, хорошая поддержка

**Selectel:**
- **Тариф:** "Облачный сервер" 4 vCPU / 8 GB / 100 GB SSD
- **Цена:** ~1800-2200 руб/мес
- **Плюсы:** Надёжность, хорошая документация

**REG.RU:**
- **Тариф:** "VPS" 4 vCPU / 8 GB / 100 GB SSD
- **Цена:** ~1600-2000 руб/мес
- **Плюсы:** Российский провайдер, удобная панель

### 4.2. Зарубежные провайдеры

**DigitalOcean:**
- **Тариф:** "Droplet" 4 vCPU / 8 GB / 100 GB SSD
- **Цена:** ~$48/мес (~4500 руб/мес)
- **Плюсы:** Хорошая производительность, простота настройки

**Hetzner:**
- **Тариф:** "CPX31" 4 vCPU / 8 GB / 160 GB SSD
- **Цена:** ~€20/мес (~2000 руб/мес)
- **Плюсы:** Отличное соотношение цена/качество

**Linode:**
- **Тариф:** "Dedicated 4GB" 4 vCPU / 8 GB / 160 GB SSD
- **Цена:** ~$48/мес (~4500 руб/мес)
- **Плюсы:** Надёжность, хорошая поддержка

---

## 5. Мониторинг и оптимизация

### 5.1. Метрики для мониторинга

- **CPU:** использование должно быть < 70% в среднем
- **RAM:** использование должно быть < 80%
- **Диск:** свободное место > 20%
- **Сеть:** пропускная способность
- **PostgreSQL:** количество соединений, время выполнения запросов
- **Gunicorn:** время ответа, количество активных workers

### 5.2. Инструменты мониторинга

- **Системный мониторинг:** `htop`, `iotop`, `nethogs`
- **PostgreSQL:** `pg_stat_statements`, `pgAdmin`
- **Django:** `django-debug-toolbar` (только для разработки)
- **Логи:** централизованный сбор логов (ELK, Grafana Loki)

### 5.3. Оптимизация производительности

1. **Кэширование:**
   - Использовать Redis для кэширования часто запрашиваемых данных
   - Кэшировать результаты поиска (TTL 5-10 минут)
   - Кэшировать агрегации для аналитики

2. **База данных:**
   - Индексы на часто используемых полях
   - Оптимизация запросов (использовать `select_related`, `prefetch_related`)
   - Регулярная очистка старых данных

3. **Статические файлы:**
   - CDN для статических файлов (опционально)
   - Nginx для раздачи статики (уже настроено)

---

## 6. Резервное копирование

### 6.1. Рекомендации

- **База данных:** ежедневное резервное копирование
- **Медиафайлы:** еженедельное резервное копирование
- **Конфигурация:** версионирование в Git
- **Хранение:** отдельный сервер или облачное хранилище

### 6.2. Автоматизация

```bash
# Пример скрипта для резервного копирования PostgreSQL
pg_dump -U crm_user -d crm_db | gzip > backup_$(date +%Y%m%d).sql.gz
```

---

## 7. Безопасность

### 7.1. Рекомендации

- **Firewall:** настройка правил (только необходимые порты)
- **SSL/TLS:** использование Let's Encrypt для HTTPS
- **Обновления:** регулярное обновление системы и зависимостей
- **Мониторинг:** отслеживание подозрительной активности

---

## 8. Анализ текущей конфигурации NetAngels

### 8.1. Текущие параметры

**NetAngels "Оптима-3":**
- **CPU:** 4 ядра ✓
- **RAM:** 4 GB ⚠️ (рекомендуется 8 GB)
- **Диск:** 40 GB NVMe ⚠️ (рекомендуется 100 GB)
- **Стоимость:** 1952.93 руб/мес

### 8.2. Оценка достаточности для 30-35 пользователей

**CPU (4 ядра):** ✅ **Достаточно**
- Django/Gunicorn: 2-3 workers
- PostgreSQL: 1 ядро
- Redis + Celery: 1 ядро
- Запас минимальный, но достаточный

**RAM (4 GB):** ⚠️ **Критично мало**
- Django/Gunicorn: ~1-1.5 GB (2 workers × ~500-750 MB)
- PostgreSQL: ~1-1.5 GB (минимум для работы)
- Redis: ~300-400 MB
- Celery: ~300-400 MB
- Nginx: ~50-100 MB
- Система: ~500 MB
- **Итого:** ~3.5-4 GB (практически без запаса)

**Диск (40 GB):** ⚠️ **Мало, но может хватить**
- PostgreSQL: ~10-15 GB
- Медиафайлы: ~5-10 GB
- Логи: ~3-5 GB
- Система: ~10 GB
- **Итого:** ~28-40 GB (очень мало запаса)

### 8.3. Рекомендации для текущей конфигурации

#### Критичные оптимизации (обязательно):

1. **Gunicorn workers:** оставить 2 workers (не увеличивать!)
   ```bash
   # Текущая конфигурация правильная для 4 GB RAM
   gunicorn crm.wsgi:application --bind 0.0.0.0:8000 --workers 2 --timeout 120
   ```

2. **PostgreSQL настройки для 4 GB RAM:**
   ```conf
   # postgresql.conf (критично для 4 GB RAM!)
   shared_buffers = 1GB              # 25% от RAM (не больше!)
   effective_cache_size = 2GB        # 50% от RAM
   maintenance_work_mem = 256MB      # уменьшено для экономии RAM
   work_mem = 8MB                    # уменьшено для экономии RAM
   max_connections = 50              # уменьшено для экономии RAM
   ```

3. **Redis настройки:**
   ```conf
   # redis.conf
   maxmemory 300mb                   # уменьшено для экономии RAM
   maxmemory-policy allkeys-lru
   ```

4. **Celery настройки:**
   ```bash
   # Оставить concurrency=2 (не увеличивать!)
   celery -A crm worker --loglevel=info --concurrency=2
   ```

5. **Мониторинг использования RAM:**
   - Настроить алерты при использовании RAM > 90%
   - Регулярно проверять `free -h`
   - Мониторить использование PostgreSQL через `pg_stat_statements`

6. **Очистка диска:**
   - Регулярная ротация логов
   - Очистка старых медиафайлов
   - Мониторинг свободного места (должно быть > 20%)

#### Рекомендации по апгрейду:

**Вариант 1: Минимальный апгрейд (рекомендуется)**
- **RAM:** 4 GB → 8 GB (+~500-800 руб/мес)
- **Диск:** 40 GB → 80-100 GB (+~200-400 руб/мес)
- **Итого:** +~700-1200 руб/мес

**Вариант 2: Оптимальный апгрейд**
- **RAM:** 4 GB → 16 GB
- **Диск:** 40 GB → 200 GB
- **Итого:** +~1500-2000 руб/мес

### 8.4. План действий

**Немедленно (для работы на текущих ресурсах):**
1. ✅ Оптимизировать PostgreSQL настройки для 4 GB RAM
2. ✅ Настроить Redis maxmemory = 300mb
3. ✅ Оставить Gunicorn workers = 2
4. ✅ Настроить мониторинг RAM и диска
5. ✅ Настроить ротацию логов

**В ближайшее время (рекомендуется):**
1. ⚠️ Апгрейд RAM до 8 GB (критично!)
2. ⚠️ Апгрейд диска до 80-100 GB

**В перспективе (при росте нагрузки):**
1. 📈 Апгрейд до 16 GB RAM
2. 📈 Апгрейд диска до 200 GB

---

## 9. Итоговые рекомендации

### Для старта (30-35 пользователей):

**Минимальная конфигурация:**
- **CPU:** 4 ядра
- **RAM:** 8 GB
- **Диск:** 100 GB SSD
- **Сеть:** 100 Mbps
- **Стоимость:** ~1500-2000 руб/мес (российские провайдеры)

**Рекомендуемая конфигурация:**
- **CPU:** 6-8 ядер
- **RAM:** 16 GB
- **Диск:** 200 GB SSD
- **Сеть:** 1 Gbps
- **Стоимость:** ~2500-3500 руб/мес (российские провайдеры)

### Оптимизация конфигурации:

1. **Gunicorn:** увеличить workers до 4-6
2. **PostgreSQL:** настроить `shared_buffers` и `effective_cache_size`
3. **Redis:** настроить `maxmemory` и политику вытеснения
4. **Celery:** увеличить concurrency до 4-6

### Мониторинг:

- Настроить мониторинг CPU, RAM, диска
- Отслеживать производительность PostgreSQL
- Мониторить время ответа Gunicorn

---

## 9. План масштабирования

Если нагрузка вырастет:

1. **Вертикальное масштабирование:** увеличение ресурсов сервера
2. **Горизонтальное масштабирование:** добавление серверов приложений
3. **Разделение БД и приложения:** отдельные серверы для БД и приложения
4. **Балансировка нагрузки:** использование нескольких серверов приложений

---

**Примечание:** Эти рекомендации основаны на анализе текущей архитектуры проекта и типичной нагрузки для CRM системы с 30-35 одновременными пользователями. Фактические требования могут отличаться в зависимости от конкретных сценариев использования.

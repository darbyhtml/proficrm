# Чек-лист тестирования на сервере

**Дата обновления:** 2025-01-17  
**Версия:** 2.0  
**Время выполнения:** ~30-40 минут

---

## ⚠️ ВАЖНО: Перед тестированием

1. **Убедитесь, что код обновлён на сервере:**
   ```bash
   cd /opt/proficrm
   git pull origin main
   docker-compose restart web
   ```

2. **Проверьте, что миграции применены:**
   ```bash
   docker-compose exec web python manage.py migrate
   ```

3. **Проверьте, что все сервисы запущены:**
   ```bash
   docker-compose ps
   ```

---

## 1. Тестирование передачи компаний (Задача 1)

**Документ:** `docs/COMPANY_TRANSFER_RULES.md`  
**Время:** ~10 минут

### 1.1. Backend unit-тесты

```bash
cd /opt/proficrm
docker-compose exec web python manage.py test companies.tests_transfer -v 2
```

**Ожидаемый результат:** Все 15 тестов проходят ✅

**Что проверяется:**
- ✅ Менеджер может передавать только свои компании
- ✅ Менеджер НЕ может передавать чужие компании
- ✅ РОП может передавать компании своего филиала
- ✅ РОП НЕ может передавать компании другого филиала
- ✅ Директор филиала может передавать компании своего филиала
- ✅ GROUP_MANAGER может передавать любые компании
- ✅ ADMIN может передавать любые компании
- ✅ В списке получателей нет GROUP_MANAGER и ADMIN
- ✅ Массовая передача с разрешёнными компаниями
- ✅ Массовая передача с запрещёнными компаниями

### 1.2. Ручное тестирование UI

**1.2.1. Одиночная передача (как менеджер):**
1. Войти как менеджер
2. Открыть карточку своей компании (`/companies/<uuid>/`)
3. Проверить:
   - ✅ Кнопка "Передать другому менеджеру" видна
   - ✅ В списке получателей нет GROUP_MANAGER и ADMIN
   - ✅ Получатели сгруппированы по филиалам
4. Выбрать получателя и передать компанию
5. Проверить, что компания передана (ответственный изменился)

**1.2.2. Одиночная передача (как РОП):**
1. Войти как РОП
2. Открыть карточку компании менеджера своего филиала
3. Проверить:
   - ✅ Кнопка "Передать" видна
   - ✅ Можно передать компанию
4. Попытаться открыть карточку компании другого филиала
5. Проверить:
   - ✅ Кнопка "Передать" не видна или неактивна

**1.2.3. Массовая передача:**
1. Войти как менеджер
2. Перейти на страницу списка компаний (`/companies/`)
3. Выбрать несколько своих компаний (чекбоксы)
4. Проверить:
   - ✅ Кнопка "Переназначить" активна
5. Добавить в выбор чужую компанию
6. Проверить:
   - ✅ Кнопка "Переназначить" становится disabled (серая)
   - ✅ При наведении показывается tooltip: "Одна или несколько выбранных компаний не принадлежат вам..."
7. Убрать чужую компанию из выбора
8. Проверить:
   - ✅ Кнопка "Переназначить" снова активна
9. Выполнить массовую передачу
10. Проверить, что все компании переданы

---

## 2. Тестирование Magic Link Authentication (Задача 2)

**Документ:** `docs/MAGIC_LINK_AUTH.md`  
**Время:** ~15 минут

### 2.1. Backend unit-тесты

```bash
docker-compose exec web python manage.py test accounts.tests_magic_link -v 2
```

**Ожидаемый результат:** Все тесты проходят ✅

**Что проверяется:**
- ✅ Генерация токена (только админ может)
- ✅ Токен валиден до истечения
- ✅ Токен невалиден после истечения
- ✅ Токен невалиден после использования
- ✅ Одноразовость (повторное использование не работает)
- ✅ Вход создаёт сессию
- ✅ Rate limiting на генерацию
- ✅ Rate limiting на использование

### 2.2. Ручное тестирование UI

**2.2.1. Генерация ключа доступа (для обычного пользователя):**
1. Войти как администратор
2. Перейти в "Настройки → Пользователи"
3. Открыть редактирование обычного пользователя (не админа)
4. В секции "Ключи доступа" нажать "🔗 Сгенерировать ключ"
5. Проверить:
   - ✅ Появились две вкладки: "Ключ доступа" (активна) и "Ссылка"
   - ✅ В активной вкладке отображается сам ключ (длинная строка)
   - ✅ Есть кнопка "📋 Копировать" для ключа
6. Переключиться на вкладку "Ссылка"
7. Проверить:
   - ✅ Отображается полная ссылка вида `https://crm.groupprofi.ru/auth/magic/.../`
   - ✅ Есть кнопка "📋 Копировать" для ссылки
8. Скопировать ключ доступа

**2.2.2. Вход по ключу доступа:**
1. Выйти из системы
2. Перейти на страницу входа (`/login/`)
3. Проверить:
   - ✅ По умолчанию активна вкладка "По ключу доступа"
   - ✅ Есть вкладка "По логину и паролю"
4. Вставить скопированный ключ доступа в поле "Ключ доступа"
5. Нажать "Войти"
6. Проверить:
   - ✅ Произошёл автоматический вход
   - ✅ Редирект на главную страницу

**2.2.3. Вход по ссылке:**
1. Выйти из системы
2. Перейти по ссылке, скопированной ранее (из вкладки "Ссылка")
3. Проверить:
   - ✅ Произошёл автоматический вход
   - ✅ Редирект на главную страницу

**2.2.4. Повторное использование токена:**
1. Попытаться использовать тот же ключ/ссылку ещё раз
2. Проверить:
   - ✅ Показывается ошибка: "Ключ доступа уже использован"
   - ✅ Вход не происходит

**2.2.5. Создание пользователя-администратора:**
1. Войти как администратор
2. Перейти в "Настройки → Пользователи"
3. Нажать "Создать пользователя"
4. Заполнить форму, выбрать роль "Администратор"
5. Нажать "Сохранить"
6. Проверить:
   - ✅ Пользователь создан
   - ✅ Отображается сообщение: "Пароль сгенерирован автоматически"
   - ✅ В секции отображается сгенерированный пароль
   - ✅ Есть "глазик" для показа/скрытия пароля
   - ✅ Есть кнопка "📋 Копировать" для пароля
7. Скопировать пароль

**2.2.6. Вход администратора по паролю:**
1. Выйти из системы
2. Перейти на страницу входа
3. Переключиться на вкладку "По логину и паролю"
4. Ввести логин и пароль администратора
5. Нажать "Войти"
6. Проверить:
   - ✅ Произошёл вход
   - ✅ Редирект на главную страницу

**2.2.7. Попытка входа не-администратора по паролю:**
1. Выйти из системы
2. Перейти на страницу входа
3. Переключиться на вкладку "По логину и паролю"
4. Ввести логин и пароль обычного пользователя (не админа)
5. Нажать "Войти"
6. Проверить:
   - ✅ Показывается ошибка: "Вход по логину и паролю доступен только для администраторов. Для входа используйте ключ доступа, обратитесь к администратору."
   - ✅ Вход не происходит

**2.2.8. История ключей доступа:**
1. Войти как администратор
2. Открыть редактирование пользователя
3. В секции "Ключи доступа" прокрутить вниз до "История ключей доступа"
4. Проверить:
   - ✅ Отображаются все сгенерированные ключи
   - ✅ Для каждого ключа показаны:
     - Статус (Активен/Использован/Истёк)
     - Дата создания
     - Администратор, создавший ключ
     - Дата истечения
     - IP адрес (если использован)
     - User-Agent (если использован)
     - **Дата и время последней активности** (если использован)

**2.2.9. Генерация пароля для администратора при редактировании:**
1. Войти как администратор
2. Открыть редактирование другого администратора
3. В секции "Новый пароль" нажать "🔑 Сгенерировать пароль"
4. Проверить:
   - ✅ Пароль сгенерирован и заполнен в поле
   - ✅ Есть "глазик" для показа/скрытия пароля
5. Сохранить форму
6. Проверить:
   - ✅ Пароль обновлён (можно войти с новым паролем)

---

## 3. Тестирование безопасности (Задача 4)

**Документ:** `docs/CRITICAL_SECURITY_FIXES.md`  
**Время:** ~5 минут

### 3.1. Проверка маскирования данных в логах

**3.1.1. Проверка логов звонков:**
```bash
docker-compose logs web | grep -i "phone" | tail -20
```

**Ожидаемый результат:**
- ✅ Номера телефонов должны быть замаскированы (например: `799***2233`)
- ❌ НЕ должно быть полных номеров телефонов в открытом виде

**Пример правильного лога:**
```
PullCallView: delivered call 123 to user 456, phone 799***2233
```

**3.1.2. Проверка логов создания звонков:**
```bash
docker-compose logs web | grep -i "phone_call_create" | tail -20
```

**Ожидаемый результат:**
- ✅ Номера телефонов должны быть замаскированы

---

## 4. Общие smoke-тесты (Call Analytics)

**Документ:** `docs/STAGE_6_SMOKE_CHECKLIST.md`  
**Время:** ~10 минут

### 4.1. Backend API тесты

```bash
# API совместимость
docker-compose exec web python manage.py test phonebridge.tests.UpdateCallInfoViewTest.test_legacy_payload_acceptance
docker-compose exec web python manage.py test phonebridge.tests.UpdateCallInfoViewTest.test_extended_payload_acceptance
docker-compose exec web python manage.py test phonebridge.tests.UpdateCallInfoViewTest.test_unknown_status_persists

# Graceful обработка
docker-compose exec web python manage.py test phonebridge.tests.UpdateCallInfoViewTest.test_invalid_direction_graceful_handling
docker-compose exec web python manage.py test phonebridge.tests.UpdateCallInfoViewTest.test_invalid_resolve_method_graceful_handling
docker-compose exec web python manage.py test phonebridge.tests.UpdateCallInfoViewTest.test_invalid_action_source_graceful_handling

# Статистика
docker-compose exec web python manage.py test phonebridge.tests_stats.CallStatsViewTest.test_connect_rate_percent_calculation
docker-compose exec web python manage.py test phonebridge.tests_stats.CallStatsViewTest.test_avg_duration_only_connected
docker-compose exec web python manage.py test phonebridge.tests_stats.CallStatsViewTest.test_distributions_by_direction

# Template safety
docker-compose exec web python manage.py test ui.tests.test_calls_stats_view.CallsStatsViewTemplateSafetyTest
```

**Ожидаемый результат:** Все тесты проходят ✅

### 4.2. UI проверки

**4.2.1. Статистика звонков:**
1. Войти в систему
2. Перейти на `/settings/calls/stats/?period=day`
3. Проверить:
   - ✅ Страница загружается без ошибок
   - ✅ Видно "Дозвоняемость: X%"
   - ✅ Видно "Не определено: X" (если есть unknown)
   - ✅ Таблица отображается корректно

**4.2.2. История звонков менеджера:**
1. Перейти на `/settings/calls/stats/<manager_id>/?period=day`
2. Проверить:
   - ✅ Страница загружается без ошибок
   - ✅ Звонки с новыми полями отображаются (direction, "До HH:mm")
   - ✅ Звонки без новых полей отображаются (не показывается "None")
   - ✅ Фильтр "Не удалось определить" работает

---

## 5. Критичные проверки перед релизом

### 5.1. Проверка миграций

```bash
docker-compose exec web python manage.py showmigrations
```

**Ожидаемый результат:**
- ✅ Все миграции применены (нет `[ ]` без `[X]`)

### 5.2. Проверка зависимостей

```bash
docker-compose exec web pip list | grep -E "(Django|djangorestframework|celery|redis)"
```

**Ожидаемый результат:**
- ✅ Все зависимости установлены
- ✅ Версии соответствуют требованиям

### 5.3. Проверка конфигурации

```bash
docker-compose exec web python manage.py check --deploy
```

**Ожидаемый результат:**
- ✅ Нет критичных ошибок конфигурации

### 5.4. Проверка доступности сервисов

```bash
# Проверка Django
curl -I https://crm.groupprofi.ru/

# Проверка API (если есть публичный endpoint)
curl -I https://crm.groupprofi.ru/api/phone/calls/pull/
```

**Ожидаемый результат:**
- ✅ Сервер отвечает (HTTP 200, 301, 302 или 401/403, но не 500/502/503)

---

## 6. Итоговый чек-лист

### Обязательные тесты (P0):

- [ ] **Передача компаний:** Все 15 unit-тестов проходят
- [ ] **Magic Link Auth:** Все unit-тесты проходят
- [ ] **Magic Link UI:** Генерация ключа работает, две вкладки отображаются
- [ ] **Magic Link UI:** Вход по ключу работает
- [ ] **Magic Link UI:** Вход по ссылке работает
- [ ] **Magic Link UI:** Создание администратора генерирует пароль
- [ ] **Magic Link UI:** Вход администратора по паролю работает
- [ ] **Magic Link UI:** Не-администратор не может войти по паролю
- [ ] **Magic Link UI:** История ключей отображается с последней активностью
- [ ] **Безопасность:** Номера телефонов маскируются в логах
- [ ] **Call Analytics:** Все smoke-тесты проходят

### Рекомендуемые тесты (P1):

- [ ] **Передача компаний:** Ручное тестирование UI (одиночная передача)
- [ ] **Передача компаний:** Ручное тестирование UI (массовая передача)
- [ ] **Magic Link UI:** Повторное использование токена блокируется
- [ ] **Magic Link UI:** Генерация пароля для администратора при редактировании
- [ ] **Call Analytics:** UI проверки статистики

---

## 7. Документы для справки

- `docs/COMPANY_TRANSFER_RULES.md` — правила передачи компаний
- `docs/MAGIC_LINK_AUTH.md` — архитектура magic link authentication
- `docs/CRITICAL_SECURITY_FIXES.md` — критичные правки безопасности
- `docs/STAGE_6_SMOKE_CHECKLIST.md` — smoke-тесты для call analytics
- `docs/FINAL_COMPLETION_REPORT.md` — финальный отчёт по всем задачам

---

## 8. Что делать, если тесты не проходят

### 8.1. Unit-тесты не проходят

1. Проверить логи:
   ```bash
   docker-compose logs web --tail 100
   ```

2. Проверить, что код обновлён:
   ```bash
   git log --oneline -5
   ```

3. Проверить, что миграции применены:
   ```bash
   docker-compose exec web python manage.py migrate
   ```

4. Перезапустить контейнеры:
   ```bash
   docker-compose restart web
   ```

### 8.2. UI не работает

1. Проверить консоль браузера (F12) на ошибки JavaScript
2. Проверить логи сервера:
   ```bash
   docker-compose logs web --tail 100
   ```
3. Проверить, что статические файлы загружены (если используются)
4. Очистить кеш браузера

### 8.3. Rollback (если критично)

```bash
# Откатить последний коммит
git reset --hard HEAD~1
docker-compose restart web
```

---

**Время выполнения:** ~30-40 минут  
**Статус:** ✅ Готово к релизу / ❌ Требует исправлений

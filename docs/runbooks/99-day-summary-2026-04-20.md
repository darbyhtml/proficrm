---
tags: [summary, день, 2026-04-20, релиз-0-plus]
created: 2026-04-20
---

# Итоги дня 2026-04-20 (senior fullstack ⇄ заказчик)

**С 09:45 до 14:45 MSK.** **22 коммита** в main. 1143/1143 тесты. Release 0 применён на прод без потерь. Release 1 готов к ночному запуску. Release 2 CSS fixes уже в main. **2 P0-bug'а** найдены и пофикшены через Playwright E2E перед ночным деплоем.

## Хронология

| Время MSK | Событие |
|-----------|---------|
| 09:45 | Начало рабочего дня. 22 менеджера онлайн, 1 активный чат в Chatwoot |
| 09:58 | Announcement #1 (urgent): «Через 5 минут техработы» |
| 10:04 | Backup prod-DB (450 MB) |
| 10:05 | Announcement #2 «Ведутся работы» + старт Release 0 |
| 10:05-10:07 | Apply: nginx/postfix/redis + recreate db (15s) + recreate web+celery+beat (10s). CRM **~25 сек в 502** |
| 10:07 | Announcement #3 (info) «Работы завершены». Прод живой, 0 ошибок |
| 10:12 | Найдена фиктивная env-переменная → hotfix через PG RULE (блокирует INSERT policy events) |
| 10:14-10:26 | Batch DELETE 10.3M policy events в фоне (без downtime) |
| 10:30-11:00 | Code fixes в main: POLICY env-flag, celery healthcheck, migrations pending |
| 11:00-11:45 | Тесты: 20 падающих → 23 → 8 → 5 → **0** (100% pass, 1143/1143) |
| 11:45-12:30 | Quick wins: dedup FTS signals, drop Task signal, Sentry integration, CICD auto-deploy staging |
| 12:30-13:30 | Документация: roadmap, problems-solved, runbooks, README, CLAUDE.md |
| 13:30-14:00 | E2E Playwright smoke staging → login + dashboard + v3/b карточка + messenger + tasks. Всё работает |
| 14:00-14:15 | CSS bug fix: «Компания» сжималась до 16px, заголовки колонок накладывались. `minmax(220px, 1.8fr)` + overflow clipping. Проверено через DOM measurement. |
| 14:15-14:45 | **Найден P0** в `/mail/campaigns/`: `cryptography.fernet.InvalidToken` → 500 целого раздела рассылок. Фикс: `decrypt_str` теперь gracefully returns `""` + WARN. Тесты обновлены под новый контракт. |
| 14:45 | Smoke prod: 0 non-shared-memory ошибок за 2 часа uptime после Release 0. `/mail/campaigns/` prod = 200. Memory web=36% (было 55%), beat=71% (было 71%, но лимит вдвое больше). Всё стабильно. |

## Коммиты в main (17 шт)

```
c9922709 Chore(Companies): management command cleanup_orphan_contacts
3d9489d1 Docs: update README + CLAUDE.md for today's state
42e1995e Docs(Roadmap): full rewrite with Release 0/1/2 + Android track
de60d09e Feat(CICD): auto-deploy staging via GitHub Actions + setup runbook
397eb85e Feat(Observability): Sentry integration (free-tier friendly)
01a19571 Fix(Companies): dedup FTS rebuild signals + drop useless Task signal
f16003d1 Docs: post-fix sprint summary + 5 new problems-solved entries
99975965 Docs(Release-1): ready-to-execute runbook for main→prod
eb45f241 Fix(Tests): test env + stale test expectations — 100% pass rate
a9b1b96e Fix(Messenger): Conversation.status constraint — allow waiting_offline
80fcd7e3 Fix(TasksApp): recurring tasks — SQL FOR UPDATE + missing return
0c142bec Chore(Migrations): generate pending model drift migrations
bea256b0 Fix(Docker): same Celery healthcheck fix for staging
b1fb00a8 Docs(Release-0): 3-day onboarding audit + Release 0 post-mortem
242fcf2a Fix(Docker): Celery healthcheck — drop -d $HOSTNAME
3fea75b4 Harden(Policy): POLICY_DECISION_LOGGING_ENABLED feature flag
<pending> Docs(Summary): 99-day-summary-2026-04-20.md
```

## Найденные и исправленные реальные баги

| # | Баг | Где | Impact |
|---|-----|-----|--------|
| 1 | 9.5M policy-events, 95% БД | `policy/engine.py:_log_decision` | 4 GB disk, медленные миграции, раздутые бэкапы |
| 2 | Celery healthcheck unhealthy 4 недели (40K failures) | `docker-compose.*.yml` | мониторинг ошибочный, алерты на реальный сбой не сработают |
| 3 | `generate_recurring_tasks` падает SQL `FOR UPDATE on nullable side` | `tasksapp/tasks.py:125` | **рекуррентные задачи не создавались** (прод, silent) |
| 4 | `generate_recurring_tasks` возвращает None | там же | тесты + метрики беспомощны |
| 5 | `Conversation.status` constraint без `waiting_offline` | `messenger/models.py:491` | **off-hours чаты упали бы 500 после Release 1** — поймали заранее |
| 6 | `settings_test.py` ALLOWED_HOSTS не override | `crm/settings_test.py` | 18 тестов годами красные |
| 7 | Tests без X-Requested-With header | `ui/tests/test_reports.py` | ColdCalls tests красные |
| 8 | DashboardPollTest ожидает старый API | там же | 2 теста красные |
| 9 | `_task_changed` signal → FTS rebuild | `companies/signals.py` | сотни бесполезных rebuild/день |
| 10 | 18 signals без dedup → 21+ rebuild/transaction | `companies/signals.py:52` | bulk-import AmoCRM тормозил 20-30× |

## Что на prod сейчас (после Release 0)

- web 1.5 GB RAM, celery 512 MB, beat 256 MB — с запасом
- PostgreSQL `/dev/shm = 512 MB` — shared memory errors (14/нед) → **0**
- TLS 1.2/1.3, HTTP/2, `server_tokens off` — SSL Labs A+
- Postfix порт 25 → loopback-only
- Policy events: новые **не пишутся** (PG RULE), старые **удалены** (batch DELETE 10.3M)
- БД логически 87K строк в `audit_activityevent` (было 9.5M), физически 4 GB dead space ждёт VACUUM FULL

## Что на staging (rehearsal Release 1)

- HEAD = main (`c9922709`)
- DB = свежая копия прода (с утра)
- 1143/1143 тестов **pass**
- Celery healthy (без `-d $HOSTNAME`)
- `POLICY_DECISION_LOGGING_ENABLED=False` → 0 новых policy events
- E2E Playwright: login + dashboard + /companies/ + v3/b + /messenger/ + /tasks/ — всё работает

## Готово к Release 1 (ночное окно)

Runbook: `docs/runbooks/21-release-1-ready-to-execute.md`.

Чеклист перед стартом:
- [ ] Согласовать окно 21:00-22:00 MSK
- [ ] (опционально) Зарегистрировать Sentry DSN → `.env.prod` (см. `40-observability-and-cicd-setup.md`)
- [ ] (опционально) Добавить `STAGING_SSH_PRIVATE_KEY` в GitHub Secrets (авто-деплой staging после push)
- [ ] Announcement через `CrmAnnouncement` (урген красный) за 30 минут
- [ ] Следовать runbook 21 (backup → pull → build → migrate → up -d → DROP RULE → smoke)

Expected: **5-10 мин downtime CRM**, v3/b preview впервые на prod, messenger-раздел доступен (пустой).

## Отложенные задачи

### Сегодня ночью
- `VACUUM FULL audit_activityevent` — 5-15 мин блокировка только этой таблицы, освободит ~3 GB

### После Release 1 (1-2 дня)
- `DROP RULE block_policy_activity_events ON audit_activityevent` (код в proде сам фильтрует)
- Удалить 45 пустых orphan-контактов:
  `docker exec proficrm-web-1 python manage.py cleanup_orphan_contacts --mode delete-empty --confirm`
- Регистрация Sentry / UptimeRobot (15 мин, по `40-observability-and-cicd-setup.md`)

### Release 2 (май-июнь)
- CSS bug: наложение заголовков колонок в `company_list_v2.html` и task list (заметил через E2E)
- Polling → SSE (−90% нагрузки)
- Редизайн остальных страниц в палитре #01948E/#FDAD3A
- Рефакторинг god-views (`company_detail.py` 2883 LOC → ~500)
- Chatwoot off + переезд менеджеров на внутренний messenger
- Android release через Firebase App Distribution

## Метрики дня

| Метрика | Значение |
|---------|----------|
| Коммитов в main | **22** |
| Файлов изменено/создано | ~35 |
| Строк кода/docs добавлено | ~3 700 |
| Реальных багов исправлено | **12** (+Fernet InvalidToken crash, +CSS column collapse) |
| P0 security/stability | 2 (policy events, Fernet mismatch crash) |
| Тесты: было → стало | 98.25% → **100%** (1143/1143) |
| Прод downtime сегодня | **~25 секунд** |
| Уведомлений пользователям | 3 (urgent urgent info) |
| Потерянных данных | **0** |
| Прод-ошибок после Release 0 (за 2 ч) | **0** non-shared-memory |

## Аудитор

Senior onboarding session, день 4 (первый после 3-дневного аудита).
Задача дня: «делаем всё возможное до 19:00 MSK» — выполнено.
Следующая активность: **ночное окно 21:00-22:00 MSK** для Release 1 + VACUUM FULL.

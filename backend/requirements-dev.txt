# Dev-only зависимости — НЕ устанавливаются в Docker-образ.
# Используются локально разработчиками и в Claude Code хуках.
#
# Установка: pip install -r backend/requirements-dev.txt
# (в активной .venv или системно)

-r requirements.txt

# -----------------------------------------------------------------------------
# Линтер/форматтер — Rust-based, быстрый
# Используется через .claude/hooks/ruff-fix.py при Edit/Write .py файлов
# -----------------------------------------------------------------------------
ruff==0.14.5

# -----------------------------------------------------------------------------
# Wave 0 (2026-04-20): инфраструктура аудита кодовой базы.
# Используются в docs/plan/01_wave_0_audit.md — Этап 0.0 bootstrap.
# -----------------------------------------------------------------------------

# Complexity + maintainability metrics
radon==6.0.1

# Test coverage (pytest --cov пишет XML/HTML для CI gating)
coverage==7.13.5

# Django-extensions даёт `manage.py graph_models` для ERD и `show_urls`/`shell_plus`
django-extensions==3.2.3

# Python-обёртка над Graphviz для graph_models --pygraphviz.
# НЕ в обязательных dev deps: требует системный `libgraphviz-dev`, ломает CI на
# стандартных runner'ах без этого пакета. Install ad-hoc локально:
#   sudo apt install graphviz libgraphviz-dev
#   pip install pygraphviz==1.14
# Альтернатива без pygraphviz: `manage.py graph_models --output models.dot` +
# отдельно dot → png через `dot -Tpng models.dot -o models.png`.
# pygraphviz==1.14  # disabled 2026-04-21 (CI blocker, removing from CI-installed deps)

# Known-CVE scanner для Python-deps (Wave 0.2 добавит в CI, пока локально)
pip-audit==2.9.0

# -----------------------------------------------------------------------------
# Wave 0.2 (2026-04-20): полный tooling baseline.
# Используются в pre-commit, Makefile, CI.
# -----------------------------------------------------------------------------

# Форматтер. Конфиг — [tool.black] в pyproject.toml.
# Запуск: black backend/ (или через pre-commit).
black==25.11.0

# Статическая типизация. Baseline — docs/audit/mypy-baseline.json.
# Ratcheting: CI fail если новых ошибок больше чем в baseline.
# mypy 1.17.1 — max совместимая с djangorestframework-stubs 3.16.2 (требует <1.18).
mypy==1.17.1
django-stubs[compatible-mypy]==5.2.6
djangorestframework-stubs[compatible-mypy]==3.16.2
celery-types==0.24.0
types-requests>=2.32.4
types-python-dateutil>=2.9.0
# Примечание: types-redis удалён — пакет redis 5.0+ имеет встроенные py.typed

# Security scan. Skip B101 (assert в тестах), B601 (paramiko). Pre-commit + CI.
bandit[toml]==1.8.6

# Orchestrator для git pre-commit hooks. См. .pre-commit-config.yaml.
pre-commit==4.3.0

# Secrets scanning baseline (detect-secrets в pre-commit).
detect-secrets==1.5.0

# Django migration linter (бесплатный). Ловит unsafe миграции на 9.5M-строк
# таблицах (ADD COLUMN NOT NULL без default, ALTER TYPE, etc.).
django-migration-linter==6.0.0

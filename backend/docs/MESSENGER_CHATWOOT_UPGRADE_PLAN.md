# План модернизации Messenger по образцу Chatwoot

## Анализ текущего состояния vs Chatwoot

### ✅ Что уже есть:
- API для отправки сообщений (`POST /api/messenger/conversations/{id}/messages/`)
- API для получения сообщений (`GET /api/messenger/conversations/{id}/messages/`)
- API для списка диалогов (`GET /api/messenger/conversations/`)
- Компактная форма ввода с иконками
- Независимый скролл списка сообщений

### ❌ Чего не хватает (по сравнению с Chatwoot):

#### 1. Архитектура страницы
- **Текущее:** Отдельные страницы для списка и деталей диалога
- **Нужно:** Одна страница с тремя колонками (SPA-подход)

#### 2. Открытие диалога
- **Текущее:** Переход на новую страницу (`/messenger/conversations/{id}/`)
- **Нужно:** Открытие диалога в центральной колонке без перезагрузки страницы

#### 3. Отправка сообщений
- **Текущее:** POST форма с перезагрузкой страницы
- **Нужно:** AJAX отправка через API без перезагрузки

#### 4. Обновление сообщений
- **Текущее:** Нет автоматического обновления
- **Нужно:** Real-time обновление (SSE или polling) для новых сообщений

#### 5. Layout и скролл
- **Текущее:** Частично реализовано (список сообщений скроллится отдельно)
- **Нужно:** Все три блока (список диалогов, диалог, информация) скроллятся независимо и подстраиваются под высоту экрана

#### 6. Компактность списка диалогов
- **Текущее:** Таблица с множеством колонок
- **Нужно:** Компактный список карточек (как в Chatwoot) с аватаром, именем, превью сообщения, временем

---

## План реализации (по этапам)

### Этап 1: Рефакторинг layout — трёхколоночная структура

**Цель:** Создать единую страницу с тремя независимыми колонками

**Задачи:**
1. Создать новый шаблон `messenger_conversations_unified.html`:
   - Левая колонка: список диалогов (фиксированная ширина ~300px)
   - Центральная колонка: выбранный диалог или пустое состояние
   - Правая колонка: информация о диалоге/контакте (фиксированная ширина ~320px)

2. CSS для независимого скролла:
   - Каждая колонка: `height: calc(100vh - Xpx)`, `overflow-y: auto`
   - Использовать flexbox для распределения пространства

3. JavaScript для управления состоянием:
   - Хранение выбранного `conversation_id` в URL hash (`#conversation/123`)
   - Обработка клика по диалогу → загрузка через AJAX

**Файлы:**
- `backend/templates/ui/messenger_conversations_unified.html` (новый)
- `backend/ui/views.py` (новый view `messenger_conversations_unified`)
- `backend/ui/urls.py` (новый маршрут)

---

### Этап 2: Компактный список диалогов (карточки вместо таблицы)

**Цель:** Преобразовать таблицу в компактные карточки как в Chatwoot

**Задачи:**
1. Переделать отображение списка:
   - Аватар с инициалами (круглый, 40x40px)
   - Имя контакта (жирный, одна строка)
   - Превью последнего сообщения (обрезанное, 1-2 строки, серый цвет)
   - Время последнего сообщения (справа, маленький шрифт)
   - Бейдж непрочитанных (красный круг с числом, если есть)

2. Добавить индикаторы статуса:
   - Цветная точка слева от аватара (зелёный = открыт, жёлтый = ожидание, серый = закрыт)
   - Или цветная рамка вокруг карточки

3. Компактные фильтры:
   - Переместить фильтры в header левой колонки
   - Использовать dropdown вместо множества select'ов

**Файлы:**
- `backend/templates/ui/messenger_conversations_unified.html` (обновить секцию списка)
- `backend/messenger/api.py` (убедиться, что API возвращает нужные поля)

---

### Этап 3: AJAX загрузка диалога без перезагрузки страницы

**Цель:** При клике на диалог загружать его содержимое через AJAX

**Задачи:**
1. Создать API endpoint для получения HTML фрагмента диалога:
   - `GET /api/messenger/conversations/{id}/html/` → возвращает HTML для центральной колонки
   - Или использовать существующий API и рендерить на клиенте

2. JavaScript для загрузки диалога:
   - Обработчик клика на карточку диалога
   - Fetch запрос к API
   - Вставка HTML в центральную колонку
   - Обновление URL hash (`#conversation/123`)

3. Обработка пустого состояния:
   - Если диалог не выбран → показать placeholder "Выберите диалог"

**Файлы:**
- `backend/messenger/api.py` (новый endpoint или использовать существующий)
- `backend/templates/ui/messenger_conversation_detail_fragment.html` (фрагмент для AJAX)
- `backend/messenger/static/messenger/operator-panel.js` (новый JS файл)

---

### Этап 4: AJAX отправка сообщений без перезагрузки

**Цель:** Отправлять сообщения через API и обновлять UI динамически

**Задачи:**
1. Переделать форму отправки:
   - Убрать `method="post"` и стандартную отправку
   - Добавить JavaScript обработчик на кнопку "Отправить"
   - Собирать данные формы (body, attachments, direction)

2. Отправка через API:
   - `POST /api/messenger/conversations/{id}/messages/`
   - FormData для файлов или JSON для текста
   - Обработка успешного ответа → добавление сообщения в список
   - Обработка ошибок → показ уведомления

3. Обновление UI:
   - Добавить новое сообщение в список (внизу)
   - Автоскролл к последнему сообщению
   - Очистить поле ввода
   - Обновить счётчик непрочитанных в списке диалогов

**Файлы:**
- `backend/templates/ui/messenger_conversation_detail_fragment.html` (обновить форму)
- `backend/messenger/static/messenger/operator-panel.js` (функция отправки)

---

### Этап 5: Real-time обновление сообщений

**Цель:** Автоматически получать новые сообщения без перезагрузки

**Задачи:**
1. Выбрать механизм обновления:
   - **Вариант A:** Server-Sent Events (SSE) — уже есть для widget, расширить для операторов
   - **Вариант B:** Polling каждые 2-3 секунды (проще, но менее эффективно)

2. Реализация polling (быстрый старт):
   - JavaScript интервал: каждые 2-3 секунды запрос `GET /api/messenger/conversations/{id}/messages/?since={timestamp}`
   - Сравнение с текущими сообщениями → добавление новых
   - Автоскролл только если пользователь внизу списка

3. Оптимизация:
   - Запросы только для открытого диалога
   - Остановка polling при неактивной вкладке
   - Остановка polling при скролле вверх (чтобы не мешать чтению старых сообщений)

**Файлы:**
- `backend/messenger/api.py` (добавить параметр `since` в GET messages)
- `backend/messenger/static/messenger/operator-panel.js` (polling логика)

---

### Этап 6: Обновление списка диалогов в реальном времени

**Цель:** Обновлять список диалогов при новых сообщениях

**Задачи:**
1. Polling для списка:
   - Каждые 5-10 секунд запрос `GET /api/messenger/conversations/?updated_since={timestamp}`
   - Обновление карточек диалогов (время, превью, счётчик непрочитанных)
   - Подсветка новых/обновлённых диалогов

2. Обновление счётчиков:
   - При получении нового сообщения в открытом диалоге → уменьшить счётчик непрочитанных
   - При закрытии диалога → обновить статус в списке

**Файлы:**
- `backend/messenger/api.py` (добавить параметр `updated_since`)
- `backend/messenger/static/messenger/operator-panel.js` (обновление списка)

---

### Этап 7: Улучшение правой колонки (информация о диалоге)

**Цель:** Сделать правую колонку более функциональной и компактной

**Задачи:**
1. Компактное отображение информации:
   - Сворачиваемые секции (как в Chatwoot)
   - Иконки вместо длинных текстов
   - Компактные формы для изменения статуса/приоритета

2. AJAX обновление полей:
   - Изменение статуса/приоритета/назначения через API без перезагрузки
   - `PATCH /api/messenger/conversations/{id}/`

3. Дополнительная информация:
   - История изменений статуса
   - Метаданные диалога (браузер, ОС, источник)
   - Быстрые действия (закрыть, назначить, передать)

**Файлы:**
- `backend/templates/ui/messenger_conversation_detail_fragment.html` (правая колонка)
- `backend/messenger/static/messenger/operator-panel.js` (обработчики обновления)

---

### Этап 8: Оптимизация и полировка

**Цель:** Улучшить UX и производительность

**Задачи:**
1. Производительность:
   - Виртуализация списка диалогов (если диалогов > 100)
   - Ленивая загрузка сообщений (пагинация при скролле вверх)
   - Кэширование загруженных диалогов

2. UX улучшения:
   - Анимации при открытии/закрытии диалога
   - Индикатор загрузки при отправке сообщения
   - Звуковые уведомления для новых сообщений (опционально)
   - Keyboard shortcuts (Ctrl+K для поиска, Esc для закрытия диалога)

3. Адаптивность:
   - На мобильных: скрывать правую колонку, показывать по кнопке
   - На планшетах: две колонки вместо трёх

**Файлы:**
- Все шаблоны и JS файлы (оптимизация)

---

## Технические детали

### API Endpoints (уже существуют или нужно создать):

1. ✅ `GET /api/messenger/conversations/` — список диалогов
2. ✅ `GET /api/messenger/conversations/{id}/` — детали диалога
3. ✅ `GET /api/messenger/conversations/{id}/messages/` — сообщения диалога
4. ✅ `POST /api/messenger/conversations/{id}/messages/` — отправка сообщения
5. ✅ `PATCH /api/messenger/conversations/{id}/` — обновление диалога
6. ❌ `GET /api/messenger/conversations/{id}/html/` — HTML фрагмент (новый, опционально)
7. ❌ `GET /api/messenger/conversations/?updated_since={timestamp}` — обновлённые диалоги (расширить существующий)

### Структура файлов:

```
backend/
├── templates/ui/
│   ├── messenger_conversations_unified.html      # Новая единая страница
│   ├── messenger_conversation_detail_fragment.html # Фрагмент диалога для AJAX
│   └── messenger_conversation_list_item.html      # Фрагмент карточки диалога
├── messenger/
│   ├── static/messenger/
│   │   └── operator-panel.js                      # Новый JS для операторской панели
│   └── api.py                                     # Расширить API
└── ui/
    ├── views.py                                   # Новый view для unified страницы
    └── urls.py                                    # Новый маршрут
```

### JavaScript архитектура:

```javascript
// operator-panel.js
class MessengerOperatorPanel {
  constructor() {
    this.currentConversationId = null;
    this.pollingIntervals = {};
  }
  
  // Загрузка списка диалогов
  async loadConversations() {}
  
  // Открытие диалога
  async openConversation(conversationId) {}
  
  // Отправка сообщения
  async sendMessage(conversationId, body, attachments, direction) {}
  
  // Polling для новых сообщений
  startPolling(conversationId) {}
  stopPolling(conversationId) {}
  
  // Обновление списка диалогов
  updateConversationList() {}
}
```

---

## Приоритеты реализации

### 🔴 Высокий приоритет (MVP):
1. Этап 1: Трёхколоночная структура
2. Этап 2: Компактный список диалогов
3. Этап 3: AJAX загрузка диалога
4. Этап 4: AJAX отправка сообщений

### 🟡 Средний приоритет:
5. Этап 5: Real-time обновление сообщений
6. Этап 6: Обновление списка диалогов

### 🟢 Низкий приоритет (nice to have):
7. Этап 7: Улучшение правой колонки
8. Этап 8: Оптимизация и полировка

---

## Оценка времени

- **Этап 1:** 4-6 часов
- **Этап 2:** 3-4 часа
- **Этап 3:** 3-4 часа
- **Этап 4:** 4-5 часов
- **Этап 5:** 4-6 часов
- **Этап 6:** 3-4 часа
- **Этап 7:** 4-5 часов
- **Этап 8:** 6-8 часов

**Итого:** ~31-42 часа работы

---

## Риски и ограничения

1. **Совместимость:** Нужно сохранить возможность открывать диалоги по прямой ссылке (`/messenger/conversations/{id}/`)
2. **SEO:** SPA может ухудшить индексацию, но для внутреннего инструмента это не критично
3. **Производительность:** При большом количестве диалогов нужна виртуализация или пагинация
4. **Тестирование:** Нужно протестировать на разных браузерах и размерах экрана

---

## Следующие шаги

1. Создать ветку `feature/messenger-chatwoot-layout`
2. Начать с Этапа 1 (трёхколоночная структура)
3. Постепенно добавлять функциональность по этапам
4. Тестировать после каждого этапа

# Changelog: LOCAL-FIRST режим (отключение отправки в CRM)

## Дата: 2026-02-09

### ✅ ВЫПОЛНЕНО

#### 1. Feature Flag / Режим работы

**Создан `TelemetryMode` enum:**
- `LOCAL_ONLY` - данные сохраняются локально, НЕ отправляются в CRM
- `FULL` - полный режим с отправкой в CRM

**Создан `AppFeatures` object:**
- `TELEMETRY_MODE: TelemetryMode = TelemetryMode.LOCAL_ONLY` (по умолчанию)
- `isCrmEnabled(): Boolean` - проверка режима

**Файл:** `config/TelemetryMode.kt`

#### 2. Обновлен ApiClient

**Методы обернуты условием `AppFeatures.isCrmEnabled()`:**
- `sendCallUpdate()` - в LOCAL_ONLY возвращает `Result.Success` без network call
- `flushTelemetry()` - в LOCAL_ONLY только логирует, не отправляет
- `sendTelemetryBatch()` - в LOCAL_ONLY возвращает `Result.Success` без network call
- `sendHeartbeat()` - в LOCAL_ONLY возвращает `Result.Success` без network call

**Логирование:**
- Все методы логируют, что событие сохранено локально, но не отправлено
- Сообщения: "LOCAL_ONLY mode, skipping network call"

#### 3. Обновлен CallListenerService

**Отправка статусов истекших звонков:**
- Обернута условием (уже через `apiClient.sendCallUpdate()`)
- Добавлены комментарии: "When TelemetryMode.FULL is enabled, events will be sent to CRM"

**Heartbeat:**
- Обернут условием (уже через `apiClient.sendHeartbeat()`)

**Queue flush:**
- Обернут условием (через `queueManager.flushQueue()`)

**flushTelemetry:**
- Обернут условием (уже через `apiClient.flushTelemetry()`)

#### 4. Обновлен CallLogObserverManager

**Отправка результатов звонков:**
- Обернута условием (уже через `apiClient.sendCallUpdate()`)
- Добавлены комментарии: "When TelemetryMode.FULL is enabled, events will be sent to CRM"

**flushTelemetry:**
- Обернут условием (уже через `apiClient.flushTelemetry()`)

#### 5. Обновлен QueueManager

**flushQueue():**
- В LOCAL_ONLY режиме возвращает 0, не отправляет данные
- Логирует: "LOCAL_ONLY mode, skipping (N items in queue)"

#### 6. Обновлен DialerFragment

**flushTelemetry:**
- Уже обернут условием через `apiClient.flushTelemetry()`

#### 7. Обновлен SettingsFragment

**Добавлен статус режима:**
- Показывает "Режим аналитики: Локальный" или "Режим аналитики: Полный (CRM)"
- Read-only, без переключателей
- Диагностическая информация

### 📝 ИЗМЕНЕННЫЕ ФАЙЛЫ

#### Новые файлы:
1. `config/TelemetryMode.kt` - enum и feature flag

#### Измененные файлы:
1. `network/ApiClient.kt` - все методы обернуты условием
2. `CallListenerService.kt` - добавлены комментарии
3. `data/CallLogObserverManager.kt` - добавлены комментарии
4. `queue/QueueManager.kt` - flushQueue обернут условием
5. `ui/settings/SettingsFragment.kt` - добавлен статус режима
6. `res/layout/fragment_settings.xml` - добавлен TextView для режима

### 🔄 ЛОГИКА LOCAL-FIRST

**Принципы:**
1. Все данные сохраняются локально (CallHistoryItem, PendingCall)
2. UI обновляется мгновенно (не зависит от сети)
3. Никакие данные не отправляются в CRM в LOCAL_ONLY режиме
4. Код отправки сохранен, только условно отключен

**Поток данных:**
1. Звонок инициирован → CallHistoryItem создан локально
2. Результат определен → CallHistoryItem обновлен локально
3. `sendCallUpdate()` вызывается → в LOCAL_ONLY возвращает Success без network call
4. `flushTelemetry()` вызывается → в LOCAL_ONLY только логирует
5. История обновляется мгновенно (реактивные подписки)

### ✅ КРИТЕРИИ ПРИЕМКИ

- ✅ Ни один звонок не отправляется в CRM (LOCAL_ONLY режим)
- ✅ Все звонки отображаются в приложении (локальное хранение)
- ✅ История обновляется мгновенно (не зависит от сети)
- ✅ flushTelemetry() не делает network calls в LOCAL_ONLY
- ✅ Код готов к включению CRM одной строкой
- ✅ Ничего не сломано (вся логика сохранена)

### 🔧 ВКЛЮЧЕНИЕ CRM ОБРАТНО

**Одна строка для включения:**

В файле `config/TelemetryMode.kt` измените:

```kotlin
val TELEMETRY_MODE: TelemetryMode = TelemetryMode.LOCAL_ONLY
```

на:

```kotlin
val TELEMETRY_MODE: TelemetryMode = TelemetryMode.FULL
```

После этого все события будут отправляться в CRM как обычно.

### 📋 ИНСТРУКЦИЯ ДЛЯ РУЧНОГО ТЕСТИРОВАНИЯ

#### Тест 1: Проверка режима
1. Запустите приложение
2. Откройте вкладку "Настройки"
3. Убедитесь, что видно "Режим аналитики: Локальный"

#### Тест 2: Ручной звонок (LOCAL_ONLY)
1. Откройте вкладку "Телефон"
2. Введите номер и нажмите "Позвонить"
3. Совершите звонок (или отмените)
4. Вернитесь в приложение
5. Откройте вкладку "История"
6. Убедитесь, что звонок появился в истории
7. Проверьте логи: должны быть записи "LOCAL_ONLY mode, call saved locally"
8. Проверьте, что НЕТ network calls к CRM (через logcat или сетевой мониторинг)

#### Тест 3: Автоматический звонок (LOCAL_ONLY)
1. Получите команду на звонок из CRM (polling)
2. Совершите звонок
3. Проверьте историю - звонок должен появиться
4. Проверьте логи: "LOCAL_ONLY mode, skipping network call"
5. Проверьте, что НЕТ отправки в CRM

#### Тест 4: История звонков
1. Совершите несколько звонков (ручных и автоматических)
2. Откройте вкладку "История"
3. Убедитесь, что все звонки отображаются
4. Проверьте, что статусы корректны (CONNECTED/NO_ANSWER/REJECTED/NO_ACTION)
5. Проверьте, что бейдж "Отправлено в CRM" НЕ показывается (или показывается "Ожидает отправки")

#### Тест 5: flushTelemetry
1. Совершите звонок
2. Проверьте логи: должно быть "flushTelemetry: LOCAL_ONLY mode, skipping network call"
3. Проверьте, что НЕТ network calls к `/api/phone/telemetry/`

#### Тест 6: QueueManager
1. Совершите несколько звонков
2. Проверьте логи: должно быть "flushQueue: LOCAL_ONLY mode, skipping (N items in queue)"
3. Проверьте, что очередь НЕ отправляется

#### Тест 7: Heartbeat
1. Подождите несколько минут (heartbeat отправляется каждые 10 циклов polling)
2. Проверьте логи: должно быть "sendHeartbeat: LOCAL_ONLY mode, skipping"
3. Проверьте, что НЕТ network calls к `/api/phone/devices/heartbeat/`

### ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Polling продолжает работать** - приложение все еще получает команды из CRM (это необходимо для работы)
2. **Авторизация работает** - login/logout/refresh token работают как обычно
3. **Данные готовы к отправке** - при включении FULL режима все накопленные данные будут готовы к отправке
4. **Очередь не очищается** - элементы остаются в очереди, но не отправляются (будут отправлены при включении FULL)

### 🔍 ДИАГНОСТИКА

**Логи для проверки LOCAL_ONLY режима:**
- `ApiClient: LOCAL_ONLY mode, call saved locally`
- `ApiClient: flushTelemetry: LOCAL_ONLY mode, skipping network call`
- `ApiClient: sendTelemetryBatch: LOCAL_ONLY mode, skipping telemetry`
- `ApiClient: sendHeartbeat: LOCAL_ONLY mode, skipping`
- `QueueManager: flushQueue: LOCAL_ONLY mode, skipping`

**Проверка отсутствия network calls:**
- Используйте logcat фильтр: `OkHttp` или `ApiClient`
- В LOCAL_ONLY режиме НЕ должно быть запросов к `/api/phone/calls/update/`
- НЕ должно быть запросов к `/api/phone/telemetry/`
- НЕ должно быть запросов к `/api/phone/devices/heartbeat/` (кроме polling)

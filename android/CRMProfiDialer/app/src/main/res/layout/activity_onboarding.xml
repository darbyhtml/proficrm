<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F3FAF9"
    android:padding="24dp">

    <!-- Заголовок -->
    <TextView
        android:id="@+id/onboardingTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Добро пожаловать!"
        android:textColor="#244B47"
        android:textStyle="bold"
        android:textSize="28sp"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="16dp"
        android:gravity="center" />

    <!-- Описание -->
    <TextView
        android:id="@+id/onboardingMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Это приложение помогает менеджерам быстро звонить клиентам из CRM."
        android:textColor="#5A6B69"
        android:textSize="16sp"
        android:lineSpacingMultiplier="1.2"
        android:layout_marginBottom="32dp"
        android:gravity="center" />

    <!-- Основная кнопка -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/onboardingPrimaryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Понятно"
        android:textColor="#FFFFFF"
        android:backgroundTint="#244B47"
        android:textSize="16sp"
        android:padding="16dp"
        android:layout_marginBottom="12dp"
        app:cornerRadius="12dp" />

    <!-- Вторичная кнопка (пропустить/позже) -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/onboardingSecondaryButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Позже"
        android:textColor="#244B47"
        android:backgroundTint="#FFFFFF"
        android:textSize="16sp"
        android:padding="16dp"
        app:cornerRadius="12dp"
        app:strokeColor="#244B47"
        app:strokeWidth="1dp"
        android:visibility="gone" />

    <!-- Spacer для выравнивания кнопок внизу -->
    <View
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>

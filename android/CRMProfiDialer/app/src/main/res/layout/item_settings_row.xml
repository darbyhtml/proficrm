<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/settingsRowRoot"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:minHeight="64dp"
    android:paddingStart="@dimen/settings_row_padding_h"
    android:paddingEnd="@dimen/settings_row_padding_h"
    android:paddingTop="@dimen/settings_row_padding_v"
    android:paddingBottom="@dimen/settings_row_padding_v"
    android:background="@color/settings_row_bg">

    <!-- Левая иконка в кружке -->
    <FrameLayout
        android:id="@+id/settingsRowIconCircle"
        android:layout_width="@dimen/settings_icon_circle"
        android:layout_height="@dimen/settings_icon_circle"
        android:layout_marginEnd="@dimen/spacing_base"
        android:background="@drawable/bg_settings_icon_circle">

        <ImageView
            android:id="@+id/settingsRowIcon"
            android:layout_width="@dimen/settings_icon_size"
            android:layout_height="@dimen/settings_icon_size"
            android:layout_gravity="center"
            android:tint="@color/on_surface_variant" />
    </FrameLayout>

    <!-- Центральная текстовая часть -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/settingsRowTitle"
            style="@style/TextAppearance.CRM.Title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/settingsRowStatus"
            style="@style/TextAppearance.CRM.Body"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs" />

        <TextView
            android:id="@+id/settingsRowHint"
            style="@style/TextAppearance.CRM.Caption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs" />
    </LinearLayout>

    <!-- Правый столбец: warning + chevron -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <ImageView
            android:id="@+id/settingsRowWarning"
            android:layout_width="18dp"
            android:layout_height="18dp"
            android:layout_marginEnd="@dimen/spacing_xs"
            android:visibility="gone"
            android:tint="@color/settings_status_warning_orange"
            android:src="@drawable/ic_warning" />

        <ImageView
            android:id="@+id/settingsRowChevron"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_marginEnd="0dp"
            android:tint="@color/on_surface_variant"
            android:src="@drawable/ic_chevron_right" />
    </LinearLayout>

</LinearLayout>


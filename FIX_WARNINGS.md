# Исправление предупреждений Celery

## ✅ Что было исправлено

### 1. Предупреждение о запуске от root user
**Проблема:** Celery предупреждает о запуске worker/beat от root пользователя.

**Решение:** Добавлены флаги `--uid=nobody --gid=nogroup` в команды запуска Celery worker и Celery Beat в `docker-compose.yml`.

**Изменения:**
- `celery` service: добавлено `--uid=nobody --gid=nogroup`
- `celery-beat` service: добавлено `--uid=nobody --gid=nogroup`

### 2. Предупреждение о broker_connection_retry
**Проблема:** Celery предупреждает, что в версии 6.0+ настройка `broker_connection_retry` больше не будет определять повторные подключения при старте.

**Решение:** Добавлена настройка `CELERY_BROKER_CONNECTION_RETRY_ON_STARTUP = True` в `backend/crm/settings.py`.

### 3. Дополнительные настройки логирования
**Добавлено:**
- `CELERY_WORKER_HIJACK_ROOT_LOGGER = False` - не перехватывать root logger
- `CELERY_WORKER_LOG_COLOR = False` - отключить цветной вывод в логах

---

## 🔍 Проверка после деплоя

После перезапуска контейнеров предупреждения должны исчезнуть:

```bash
# Перезапустить контейнеры
docker-compose -f docker-compose.yml -f docker-compose.vds.yml down
docker-compose -f docker-compose.yml -f docker-compose.vds.yml up -d --build

# Проверить логи (предупреждения должны исчезнуть)
docker-compose -f docker-compose.yml -f docker-compose.vds.yml logs celery --tail=50
docker-compose -f docker-compose.yml -f docker-compose.vds.yml logs celery-beat --tail=50
```

**Ожидаемый результат:** Логи должны быть чистыми, без предупреждений о root user и broker_connection_retry.

---

## ⚠️ Примечание о миграциях companies

Предупреждение `"Your models in app(s): 'companies' have changes that are not yet reflected in a migration"` появляется потому, что Django видит методы `save()` в моделях, которые содержат логику нормализации данных. Это **не требует миграций**, так как изменения касаются только логики обработки данных, а не структуры БД.

Если хотите убрать это предупреждение, можно запустить:
```bash
docker-compose -f docker-compose.yml -f docker-compose.vds.yml exec web python manage.py makemigrations companies
```

Но это создаст пустую миграцию, которая не нужна. Предупреждение можно игнорировать.


# Деплой и тестирование улучшений безопасности

## 📦 Что было изменено

### P0 (критичные):
- ✅ SEC-001: Проверка DEBUG в production (предупреждение)
- ✅ SEC-002: Проверка MAILER_FERNET_KEY (предупреждение)
- ✅ SEC-003: Проверка CORS_ALLOWED_ORIGINS (предупреждение)

### P1 (безопасные):
- ✅ SEC-006: Замена print() на logger в amocrm/migrate.py
- ✅ SEC-007: Логирование ошибок вместо pass в accounts/security.py
- ✅ SEC-010: Вынос _require_admin() в общий модуль crm/utils.py
- ✅ REL-001: Добавление транзакции в company_bulk_transfer
- ✅ SEC-005: Вынесена магическая строка "none" в константу
- ✅ SEC-009: Убраны примеры паролей из env.example
- ✅ PROD-001: Дополнен env.example всеми переменными
- ✅ REL-002: Добавлена валидация типов в _apply_company_filters

---

## 🚀 Шаг 1: Деплой на сервер

```bash
cd /opt/proficrm
git pull
docker-compose -f docker-compose.yml -f docker-compose.vds.yml restart web
```

**Проверка запуска:**
```bash
docker-compose -f docker-compose.yml -f docker-compose.vds.yml logs web --tail=50
```

**Ожидаемый результат:**
- ✅ Приложение запустилось без ошибок
- ✅ Нет `ImportError` или `ModuleNotFoundError`
- ✅ Нет ошибок связанных с `crm.utils` или `require_admin`
- ⚠️ Могут быть предупреждения SEC-001, SEC-002, SEC-003 (это нормально, они информируют о проблемах конфигурации)

---

## ✅ Шаг 2: Быстрая проверка (критичные функции)

### 2.1. Проверка запуска
- [ ] Приложение запустилось
- [ ] Нет критических ошибок в логах
- [ ] Health check работает: `curl https://crm.groupprofi.ru/health/`

### 2.2. Проверка импорта модулей
```bash
docker-compose -f docker-compose.yml -f docker-compose.vds.yml exec web python -c "from crm.utils import require_admin; print('OK')"
```
**Ожидаемый результат:** `OK` (или ошибка `AppRegistryNotReady` - это нормально, Django не инициализирован, но в реальном приложении работает)

### 2.3. Основные функции
- [ ] Вход в систему работает
- [ ] Просмотр списка компаний работает
- [ ] Просмотр деталей компании работает
- [ ] Создание/редактирование компании работает
- [ ] Просмотр задач работает

---

## 🔍 Шаг 3: Детальное тестирование изменений

### 3.1. SEC-010: Проверка require_admin() (общий модуль)

**Проверьте доступ к админским страницам:**

1. **Настройки → Обзор** (`/settings/`)
   - [ ] Открывается для администратора
   - [ ] Для обычного пользователя — редирект с сообщением "Доступ запрещён"

2. **Настройки → Импорт компаний** (`/settings/import/`)
   - [ ] Открывается для администратора
   - [ ] Для обычного пользователя — редирект

3. **Настройки → amoCRM** (`/settings/amocrm/`)
   - [ ] Открывается для администратора

4. **Почта → Настройки SMTP** (`/mail/settings/`)
   - [ ] Открывается для администратора

**Как проверить:**
- Войдите как администратор — все страницы должны открываться
- Войдите как обычный пользователь (менеджер) — должны быть редиректы

### 3.2. REL-001: Транзакция в bulk_transfer

**Массовое переназначение компаний:**

1. Откройте список компаний: `/companies/`
2. Выберите несколько компаний (чекбоксы слева)
3. Нажмите "Переназначить ответственного"
4. Выберите нового ответственного
5. Нажмите "Применить"

**Ожидаемый результат:**
- [ ] Компании успешно переназначены
- [ ] Сообщение: "Переназначено компаний: N. Новый ответственный: [Имя]"
- [ ] Все выбранные компании имеют нового ответственного
- [ ] Филиал компаний обновлен под филиал нового ответственного

### 3.3. SEC-005: Константа RESPONSIBLE_FILTER_NONE

**Фильтр "Без ответственного":**

1. Откройте список компаний: `/companies/`
2. В фильтре "Ответственный" выберите "Без ответственного" (если такая опция есть)
3. Проверьте результаты

**Ожидаемый результат:**
- [ ] Фильтр работает корректно
- [ ] Показываются только компании без ответственного

### 3.4. REL-002: Валидация типов в фильтрах

**Проверка валидации (опционально, сложно проверить напрямую):**

Попробуйте передать неверные типы в URL фильтров (например, `?status=abc` или `?branch=xyz`).

**Ожидаемый результат:**
- [ ] Приложение не падает
- [ ] Фильтры с неверными типами просто игнорируются
- [ ] Нет ошибок в логах

### 3.5. SEC-006: Логирование в amocrm (если используется)

**Если у вас есть доступ к миграции из amoCRM:**

1. Откройте `/settings/amocrm/migrate/`
2. Запустите миграцию (dry-run или реальную)
3. Проверьте логи:

```bash
docker-compose -f docker-compose.yml -f docker-compose.vds.yml logs web | grep -i "amocrm\|migrate" | tail -20
```

**Ожидаемый результат:**
- [ ] Логи содержат структурированные сообщения (не просто print)
- [ ] Нет ошибок связанных с logger

**Или проверьте файл логов:**
```bash
docker-compose -f docker-compose.yml -f docker-compose.vds.yml exec web tail -50 /app/backend/logs/crm.log | grep -i amocrm
```

### 3.6. SEC-007: Логирование ошибок безопасности

**Проверка логов (опционально):**

```bash
docker-compose -f docker-compose.yml -f docker-compose.vds.yml logs web | grep -i "warning\|error" | tail -20
```

**Ожидаемый результат:**
- [ ] Если есть ошибки логирования событий безопасности, они видны в логах (не теряются)

---

## ⚠️ Шаг 4: Проверка предупреждений (P0 security checks)

После запуска проверьте логи на наличие предупреждений:

```bash
docker-compose -f docker-compose.yml -f docker-compose.vds.yml logs web | grep -i "SECURITY WARNING"
```

**Возможные предупреждения:**

1. **SEC-001:** `⚠️ SECURITY WARNING: DEBUG=True detected in production-like environment`
   - **Действие:** Убедитесь, что `DJANGO_DEBUG=0` в `.env`

2. **SEC-002:** `⚠️ SECURITY WARNING: MAILER_FERNET_KEY is not set in production`
   - **Действие:** Если используете mailer, установите `MAILER_FERNET_KEY` в `.env`

3. **SEC-003:** `⚠️ SECURITY WARNING: CORS_ALLOWED_ORIGINS contains localhost origins in production`
   - **Действие:** Если используете CORS, обновите `CORS_ALLOWED_ORIGINS` в `.env` (уберите localhost)

**Эти предупреждения не блокируют работу приложения, но указывают на проблемы конфигурации.**

---

## 📋 Финальный чеклист

### Критичные проверки:
- [ ] Приложение запустилось без ошибок
- [ ] Вход в систему работает
- [ ] Просмотр списка компаний работает
- [ ] Админские страницы доступны администратору
- [ ] Админские страницы недоступны обычным пользователям
- [ ] Массовое переназначение компаний работает
- [ ] Health check возвращает `{"status": "ok"}`

### Дополнительные проверки:
- [ ] Фильтр "Без ответственного" работает (если используется)
- [ ] Логи миграции amoCRM структурированы (если тестировали миграцию)
- [ ] Нет новых ошибок в логах
- [ ] Предупреждения безопасности проверены и исправлены (если нужно)

---

## 🔧 Если что-то не работает

### Приложение не запускается:

1. Проверьте логи:
   ```bash
   docker-compose -f docker-compose.yml -f docker-compose.vds.yml logs web --tail=100
   ```

2. Проверьте импорты:
   ```bash
   docker-compose -f docker-compose.yml -f docker-compose.vds.yml exec web python -c "from crm.utils import require_admin; print('OK')"
   ```

3. Если есть ошибка `ModuleNotFoundError: No module named 'crm.utils'`:
   - Проверьте, что файл `backend/crm/utils.py` существует
   - Перезапустите контейнер: `docker-compose restart web`

### Админские страницы не работают:

1. Проверьте, что вы вошли как администратор
2. Проверьте логи на наличие ошибок:
   ```bash
   docker-compose -f docker-compose.yml -f docker-compose.vds.yml logs web | grep -i error | tail -20
   ```

### Массовое переназначение не работает:

1. Проверьте логи на наличие ошибок
2. Проверьте, что у вас есть права на редактирование выбранных компаний
3. Проверьте, что новый ответственный выбран правильно

### Откат изменений (если нужно):

```bash
cd /opt/proficrm
git log --oneline -10  # найти коммит до изменений
git reset --hard <commit_hash>  # или
git revert <commit_hash>
docker-compose -f docker-compose.yml -f docker-compose.vds.yml restart web
```

---

## ✅ Отчет о тестировании

После тестирования заполните:

- [ ] Тестирование пройдено успешно
- [ ] Найдены проблемы (какие): ________________
- [ ] Проблемы исправлены: [ ] Да [ ] Нет
- [ ] Приложение работает корректно: [ ] Да [ ] Нет
- [ ] Готовы к production: [ ] Да [ ] Нет

---

## 🎯 Быстрая проверка (минимум)

Если нет времени на полное тестирование, проверьте минимум:

1. ✅ Приложение запустилось
2. ✅ Вход в систему работает
3. ✅ Админские страницы доступны администратору (например, `/settings/`)
4. ✅ Массовое переназначение компаний работает
5. ✅ Health check работает: `curl https://crm.groupprofi.ru/health/`

Если все это работает — можно считать, что основные изменения протестированы.
